#!/usr/bin/env bash
# This script lists the content of the current directory after the first hyphen of the filenames using a for loop

for file in *; do
  # Skip hidden files (they don't match '*')
  # Extract and print part after the first '-'
  echo "${file#*-}"
done
